# Core dependencies (required)
fastapi==0.109.0
uvicorn[standard]==0.27.0
httpx==0.26.0
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
numpy==1.26.3

# ML dependencies (required for advanced predictions)
scikit-learn==1.4.0
pandas==2.2.0
joblib==1.3.2

# Optional: Advanced ML (install if you want transformer models)
# Uncomment to enable:
# transformers==4.36.2
# torch==2.1.2 --index-url https://download.pytorch.org/whl/cpu
# sentence-transformers==2.3.1

# Optional: For time-series models
# statsmodels==0.14.1
# prophet==1.1.5

# Optional: For Ollama integration (no install needed, just run ollama serve)
# requests  # Already included above via httpx

# For model persistence
# pickle is built-in to Python
